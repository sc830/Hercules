import React from 'react';
import { View, Text } from 'react-native';

const LunchScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Lunch Screen</Text>
      {/* Add content for your Lunch screen */}
    </View>
  );
};

export default LunchScreen;
